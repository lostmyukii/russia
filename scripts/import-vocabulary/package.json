{
  "name": "@russian-wordscodex/import-vocabulary",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "generate:domain": "tsx src/generate-domain-vocabulary.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "validate": "tsx src/cli.ts"
  },
  "dependencies": {
    "@russian-wordscodex/domain": "workspace:*",
    "csv-parse": "^6.2.1"
  },
  "devDependencies": {
    "tsx": "^4.22.4"
  }
}
